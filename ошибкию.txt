1. main.py
  - create_tables() - в main() - нарушение слоев
    main() - инфрастурктура/запуск лезет в низкий уровень repositories
    решение:
    init.py    - отдельный модуль с созданием таблиц при запуске (теперь слой в нужном месте)
                main() теперь не знает про repositories, можно добавить потом миграции
                Alembic

  - Dispatcher() - создается глобально
    проблема:
    - сложно тестировать
    - глобальное состояние
    - нельзя внедрять зависимости (sevice )
    решение:
    - сделать функцию сборки приложения и вызывать в main()

  - dp.include_router(expenses_router) - Нет DI (dependency injection) (зависимостей)
    плохо:
    - жествкая зависимость
    - нельзя подменить на тестах
    решение:
    - dp["expense_service"] = ExpenseService()
